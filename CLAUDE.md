# CLAUDE.md

このファイルは、このリポジトリで Claude Code / AI アシスタントが作業する際の指示書です。プロジェクトの一貫性と安全な運用を維持するため、以下のルールを厳守してください。

## 1. 基本方針

- 変更は小さく、意図が明確な単位で行う
- 既存のコーディング規約とプロジェクト構成を優先する
- 業務上の秘匿情報や認証情報をコードやコミットに含めない
- 変更後は必ず動作確認を行い、必要に応じてテスト・検証を実施する
- 最終的に成果物を残す前に、GitHub への反映を完了している状態にする

## 2. Git 運用ルール

以下は最重要ルールです。

### 2.1 コード変更ごとに GitHub へ push する

- コードを1回でも変更した場合は、必ずその変更をコミットし、GitHub に push する
- 変更が完了した時点でローカルのみの未 push 状態を残さない
- 作業完了時の最終確認として、git status を確認し、未追跡・未コミット・未 push の状態がないことを確認する
- 変更のたびにブランチを確認し、対象ブランチに対して push する

### 2.2 必須手順

以下の流れを守ること:

1. 作業前に現在のブランチと状態を確認する
2. 変更を加える
3. 必要に応じてテスト/検証を実行する
4. git status で差分を確認する
5. git add で対象ファイルをステージングする
6. 意味のあるコミットメッセージで git commit を実行する
7. git push origin <branch-name> を実行して GitHub に反映する
8. 最終報告時に、ブランチ名と push 済みの状態を明示する

### 2.3 コミットメッセージ規約

- 例: `feat: add property listing page`
- 例: `fix: resolve login validation issue`
- 例: `docs: update CLAUDE instructions`
- 例: `chore: update dependencies`
- 変更内容が明確に伝わる短い日本語または英語のメッセージを使用する

### 2.4 例外条件

- GitHub に push できない場合、以下を明確に報告する:
  - どの変更が未 push か
  - push できない理由
  - 次のアクションが必要か
- その場合でも、作業完了を宣言する前に、push の失敗を明示し、ユーザーへの確認を行う

## 3. 開発時の作業ルール

- 実装前に、既存コードの意図と命名規則を確認する
- 重複するロジックの修正や、無関係なリファクタリングは避ける
- 依存関係や設定ファイルの変更は、関連する説明とその影響範囲を記録する
- 破壊的変更を行う場合は、事前にユーザーへ通知する
- 変更後は、可能な限り最小の検証を行う

## 4. 完了条件

以下を満たしたときに作業完了とみなす:

- 変更が実装されている
- 必要な検証を実施している
- 変更がコミットされている
- GitHub に push 済みである
- 最終状態がユーザーに説明可能である

## 5. 重要な禁止事項

- 変更をコミットせずに放置すること
- GitHub への push をせずに完了報告すること
- 機密情報やトークンをリポジトリに含めること
- 「動いたっぽい」だけで確認せず、検証を省略すること

## 6. 実行例

```bash
git status
git add .
git commit -m "feat: implement requested change"
git push origin HEAD
```

このルールは、プロジェクトの透明性と安全性を確保するために適用されます。特に、コードを書いた後は GitHub への反映を忘れず、未 push の作業を残さないことが必須です。
